Hash2Hash_Compare.py

A simple Python3 script to compare a list of hash values of executable files from a suspect system and compare the hashes to a known reference set and print a report of all the executable files not found in the reference set.

Main purpose - DFIR: eliminate known good files from a list of files and speed up the processing time on the remaining files.
